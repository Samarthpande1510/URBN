# Copy to frontend/.env.local on the server and fill in the real IP (or domain later).
# NOTE: this is baked in at `npm run build` time — rebuild after changing it.
NEXT_PUBLIC_API_URL=http://<NEW_SERVER_IP>:8000
